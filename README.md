# Лабораторна робота № 3: Використання CSS-стилів та адаптивної верстки + Git (Командна робота)

## Учасники

Для виконання лабораторної роботи було здійснено розподіл на наступних учасників:

| Учасник | Ім'я та прізвище |
| :--- | :--- |
| 1 | Чернега Сергій |
| 2 | Гевко Андрій |
| 3 | Ремінський Максим |

## Початкова конфігурація

### Усім учасникам:

| Завдання | Команда Git |
| :--- | :--- |
| Налаштувати інформацію користувача | `git config --global user.name "Ваше Ім’я"` <br> `git config --global user.email "your.email@example.com"` |

### Тільки для Учасника 1 (ініціатор репозиторія):

| Завдання | Команда Git |
| :--- | :--- |
| Створити репозиторій у папці «Прізвище студента» | `git init` |
| Створити віддалений репозиторій (GitHub) та підключити його | `git remote add origin git@github.com:username/repository-name.git` |

### Для Учасників 2 та 3:

| Завдання | Команда Git |
| :--- | :--- |
| Клонувати репозиторій | `git clone git@github.com:username/responsive-layout-lab3.git` |

---

## УЧАСНИК 1

### Завдання для першого учасника:

| Завдання | Команда Git |
| :--- | :--- |
| Створити гілку | `git checkout -b feature/html-layout` |
| Створити файл `index.html` з повноцінною семантичною структурою | `header`, `nav`, `main`, `footer` |
| Створити розділи: **Особливості**, **Освітні програми**, **Контакти** | - |
| Додати заголовки, абзаци, списки, текстове наповнення | - |
| Зробити коміт | `git commit -m "Створено HTML-структуру сторінки та базовий контент"` |
| Відправити гілку | `git push origin feature/html-layout` |

---

## УЧАСНИК 2

### Завдання для другого учасника:

| Завдання | Команда Git |
| :--- | :--- |
| Оновити main перед роботою | `git pull origin main` |
| Створити гілку | `git checkout -b feature/css-styling` |
| Створити файл `style.css` | - |
| Підключити стилі до HTML (одним рядком у `<head>`) | - |
| Реалізувати **mobile-first**, **flex/grid**, **медіа-запити** | - |
| Додати ефекти: `hover`, `transition`, `box-shadow`, `animation` | - |
| Зробити коміт | `git commit -m "Додано стилі та адаптивну верстку"` |
| Відправити гілку | `git push origin feature/css-styling` |

**Єдине, що Учасник 2 додає в HTML:**  
`<link rel="stylesheet" href="style.css">`

---

## УЧАСНИК 3

### Завдання для третього учасника:

| Завдання | Команда Git |
|------|------|
| Оновити main перед роботою | `git pull origin main` |
| Створити гілку | `git checkout -b feature/assets` |
| Додати форму зворотного зв’язку (input text, email, textarea, button) | - |
| Додати iframe Google Maps у розділ Контакти | - |
| Додати іконки FontAwesome та графічні елементи | - |
| Додати зображення та текстове наповнення карток програм | - |
| Використати доступні URL або локальні файли | - |
| Зробити коміт | `git commit -m "Додано форму, Google Maps, іконки та зображення"` |
| Відправити гілку | `git push origin feature/assets` |

---

## Примітки:

1. Перед роботою кожен учасник виконує: `git pull origin main`
2. Після завершення — створює **Pull Request у main**
3. Коміти пишуться **українською**, коротко і по суті
4. Обов’язкові вимоги:
   - валідна HTML-структура
   - BEM-класи
   - Mobile-first стиль
5. Заборонено:
   - inline-стилі
   - `!important`
   - селектори тегів
   - статичні ширини у px
